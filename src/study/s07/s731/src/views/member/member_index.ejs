<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>

    <body>
        <%- include("../header") %>
        <h1>LIST</h1>
        <% list.forEach( (member) => { %>
            id : <%= member.ID %><br>
            pwd : <%= member.PWD %><br>
            addr : <%= member.ADDR %><br>
            name : <a href="/member/member_view/<%= member.ID %>"> <!-- member_view?id=<%= member.ID %> -->
                    <%= member.NAME %>
                   </a><hr>
        <% }) %>
        <a href="/member/register_form">데이터 추가</a>
    </body>
</html>
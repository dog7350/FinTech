<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <h3>test</h3>
        <% for (i = 0; i < member.length; i++) { %>
            <b><%= member[i].id %></b> : <%= member[i].name %> - <%= member[i].addr %><br>
        <% } %>
        <br><hr><br>
        <% member.forEach( mem => { %>
            <b><%= mem.id %></b> : <%= mem.name %> - <%= mem.addr %><br>
        <% } ) %>
    </body>
</html>